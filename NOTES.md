# 额外说明

## 多模态

默认情况下，应用会自动检索 GGUF 文件所在目录下的 mmproj 文件（例如 `Qwen3.6-27B/Qwen3.6-27B.gguf`，会在同目录查找 mmproj）。找到后前端会标记为多模态模型，具备视觉能力。

但如果你有多个同架构的微调模型（例如 `Qwen3.6-27B-威力加强版.gguf`），它们共用同一个 mmproj 文件。按「同目录」检索就需要为每个变体都复制一份 mmproj，很麻烦。因此加载参数中额外提供了一个 mmproj 文件选择项，允许手动指定任意位置的 mmproj 文件。

## MTP

llama.cpp 支持两种 MTP 使用方式：

1. **自带 MTP 层的模型** — 直接加载使用，无需额外配置
2. **额外加载 MTP 层** — 通过加载参数「草稿模型」指定一个独立的 GGUF 文件（仅含 MTP 层权重）来启用

第二种方式的用途：很多变体模型的作者并没有量化带有 MTP 层的版本，此时想启用 MTP，只能从原始模型中提取 MTP 层，单独配合变体模型加载使用（至于变体模型 + 原版 MTP 层是否影响生成质量，优质回答：我不知道）。

## 系统信息工具

系统信息页面使用 gpu-info 工具获取硬件信息（CPU、内存、磁盘、GPU 等）。可执行文件位于 `classes/tools/easy-tools/` 目录下。

这个工具的项目地址：https://github.com/IIIIIllllIIIIIlllll/Easy-GPU-Tools
程序是通过GitHub的cli编译得到的，理论上不存在安全风险（可以检查代码），我会下载下来放在该项目中，避免 Java + C 混合编译。
但是Windows平台依然有各种误报、当作病毒的可能，对此我没有办法，如果有好的建议就请告诉我吧。

## 自动加载模型

开启后，当 `/v1/chat/completions` 等 API 请求某模型但当前未加载时，服务端将自动执行以下步骤：

**① 策略检查：** 确认该模型的自动加载策略为「允许」，否则直接返回 404。

**② 硬件资源检查：** 通过 `gpu-info` 获取系统可用内存，通过 `llama-fit-params` 估算模型所需显存。Tensor 并行模式仅检查 VRAM，其他模式检查 RAM + VRAM 总和。要求可用内存 ≥ 估算值 × 1.1（留 10% 余量）。任何检查失败均拒绝加载。

> ⚠ **重要警告：** `llama-fit-params` 无法计算以下组件的显存占用：
> - 多模态（mmproj）：视觉投影器模型
> - 草稿模型（Speculative Decoding）
> - MTP（Multi-Token Prediction）
>
> 因此如果你启用了上述任一功能，硬件资源检查的结果将严重不准确。实际显存占用会高于估算值，可能导致 OOM。使用这些功能时请格外谨慎，务必手动预留足够显存。

**③ 自动加载：** 使用保存的启动配置（`launch_config.json` 中 `selectedConfig` 对应的配置）异步加载模型。

**④ 阻塞等待：** 轮询等待模型加载完成，最长等待 `autoLoadTimeoutMs`（默认 120 秒）。超时则放弃当前请求，但不中断后台加载进程。

**⑤ 转发请求：** 模型加载成功后，将原始 API 请求转发给模型。

- **重复请求处理：** 多个请求同时触发同一模型的自动加载时，仅第一个提交加载任务，其余等待同一个加载完成。
- **永远不会自动卸载模型，** 卸载必须由用户手动操作。

> **注意：** 你需要为模型设置一个可用的启动配置才能启用自动加载模式，否则自动加载时将找不到明确的启动配置。

一切后果自负。

## 模型路径

1. 默认情况下，程序会自动扫描根目录下的 models 文件夹来查找模型，不需要再添加这个路径了；
2. 不同的模型不要放在一个目录里，单独给他们设置一个文件夹，多模态模型的话，mmproj文件也放在一起；
3. **模型文件夹名称唯一**：该程序使用模型所在的文件夹名称作为模型的唯一标识（modelId），请避免在不同路径下出现同名文件夹。例如 `D:\Models\Qwen3.6-27B\model.gguf` 和 `D:\llama.cpp-hub\models\Qwen3.6-27B\aaa.gguf` 会导致 modelId 冲突，影响外部客户端调用。请确保每个模型文件夹名称在整个搜索路径中唯一。
4. 暂时没了。

## 用量统计

1. 这玩意只能统计 **正常完成响应的用量** ，如果某次请求被中断了，那就统计不到token用量了；
2. 远程节点的用量也是统计不到的，如果你在节点A上用了100万token，然后把节点A聚合到了节点B，通过B去访问节点A，那么在B看来，用量是0；
3. 暂时没了。

## 磁盘与内存占用

### 内存

该应用使用 Java 开发，绕不开 JVM 这个内存消耗大户。已尽可能地优化了内存使用，JVM 默认的堆内存只有 96MB（未来还会继续改小，作者自用 64MB）。

代价是：如果请求的内容太大，就会走磁盘交换。也就是说，如果有人喜欢折腾，故意提交了一些超大的请求，该程序会把请求塞进磁盘作为缓冲，避免 JVM 内存爆炸。

### 磁盘

应用本体不算很大，但 llama.cpp 往往很大，尤其是 CUDA 和 ROCm 的预编译包，解压后接近 1GB。再加上前面提到的磁盘缓存逻辑，建议将该应用放置在磁盘空间充裕的位置，至少预留 **2GB** 可用空间。

**用量统计** 占用的磁盘空间不大，每条请求记录仅 55 字节，一百万条请求日志约 55MB。本地部署的模型想要产生一百万条请求，得用很久，所以问题不大。

**系统日志** 可能会占用一些空间，但最长只保留 7 天。如果觉得太长，可以去 `log4j.xml` 里修改。

## 远程节点

支持将多个 llama.cpp-hub 服务实例聚合到一起统一管理，通过主从节点架构将多台服务器的模型列表、运行状态聚合到主节点前端。

**节点角色：**
- **主节点（master）**：管理所有从节点的 WebSocket 连接，运行 30 秒健康检查，中继日志和事件
- **从节点（slave）**：单机普通节点，不发起远程连接/健康检查；默认（未设置）即视为从节点。其自身仍可被别的主节点聚合

**远程路由（3 层查找）：**
1. 请求体中显式指定 `nodeId` → 直接路由到该节点
2. 本地已加载模型 → 本地处理
3. 全节点回退 → 遍历所有已启用的远程节点查询 `/v1/models`

**WebSocket 事件中继：**
- 远程节点的系统日志、模型加载/停止事件、模型繁忙状态等自动中继到主节点前端
- 远程日志行显示 `[nodeId/modelId]` 前缀
- 前端模型列表支持节点筛选下拉框（全部/本地/远程）

**配置方式（全部在 Web 界面操作）：**
- **切换角色**：WebUI → 系统设置 → 节点 tab 顶部「本节点角色」下拉框选择「主节点」/「普通节点」→ 点「保存」→ **重启服务生效**
  - 切换为从节点时，已配置的远程节点会被**冻结**（保留、只读可见，但不建立 WebSocket 连接、不参与健康检查与路由）；切回主节点即恢复
  - 角色选择仅写入 `application.json` 的 `"nodeRole"` 字段，运行时的健康检查调度器/WebSocket 客户端不会随切换而启停，故需重启
- **添加/编辑/删除节点**：同一设置页 → 节点 tab，需本机角色为主节点方可操作
- **节点信息**：`nodeId`、`name`、`baseUrl`，可选 `apiKey`，全部在面板里添加
- 节点列表独立存储于 `config/nodes.json`，与角色配置分离

## 多协议 API

| 协议 | 端口 | 状态 |
|------|------|------|
| OpenAI | 8080 | ✅ |
| Anthropic | 8080 | ✅ |
| Ollama | 11434 | ⚠️ 默认关闭，需手动启用 |
| LM Studio | 1234 | ⚠️ 默认关闭，需手动启用 |

**LM Studio 兼容说明：** 本项目仅实现以下端点：

| 状态 | 端点 | 说明 |
|------|------|------|
| ✅ | `/api/v0/models` | 模型列表 |
| ✅ | `/api/v0/chat/completions` | 聊天补全 |
| ✅ | `/api/v0/completions` | 文本补全 |
| ✅ | `/api/v0/embeddings` | 嵌入向量 |
| ✅ | `/v1/models` | OpenAI 格式模型列表 |
| ✅ | `/v1/chat/completions` | OpenAI 格式聊天补全 |
| ✅ | `/v1/completions` | OpenAI 格式文本补全 |
| ✅ | `/v1/embeddings` | OpenAI 格式嵌入向量 |
| ✅ | `/v1/rerank` | OpenAI 格式重排序 |

以下 LM Studio 自有端点 **不兼容**，不再维护：

| 端点 | 方法 |
|------|------|
| `/api/v1/chat` | POST |
| `/api/v1/models` | GET |
| `/api/v1/models/load` | POST |
| `/api/v1/models/unload` | POST |
| `/api/v1/models/download` | POST |
| `/api/v1/models/download/status` | GET |

## 安全验证

### API Key 保护

在 `application.json` 中启用 API Key 后，**所有路径**（包括 WebUI）都需要验证：

```json
{
  "security": {
    "apiKeyEnabled": true,
    "apiKey": "your-secret-key-here"
  }
}
```

| 路径 | 验证方式 |
|------|---------|
| `/v1/*` | `Authorization: Bearer <key>` 或 `x-api-key: <key>` |
| WebUI、`/api/*`、管理接口 | `Authorization: Bearer <key>`（无 key 时返回登录页，输入后存 Cookie 自动注入） |

- 浏览器访问 WebUI 时显示登录页，输入 API Key 后自动存入 Cookie，后续请求自动带 Bearer token
- API 调用方使用 Bearer token 或 x-api-key
- 使用常量时间比较（`MessageDigest.isEqual`），防止计时攻击
- IP 级别暴力破解防护：连续 5 次验证失败后封禁 15 分钟
- 客户端 IP 直接从 TCP 连接提取，不信任 `X-Forwarded-For` / `X-Real-IP` 等可伪造的代理头
- `/v1/chat/completions` 等流式接口、WebSocket 握手、文件上传/下载均统一走 `ApiKeyValidator` 鉴权
- `/api/sys/setting` 和 `/api/cert/status` 不再返回明文 apiKey / keystorePassword
